model InvoiceDetail {
    id          Int     @id @default(autoincrement())
    uuid        String  @unique @default(uuid())
    name        String
    quantity    Int
    invoices    Invoice[]
    price       Float
}